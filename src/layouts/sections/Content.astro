---
export interface Props {
	id: string;
	class: string;
}

const { id, class: className } = Astro.props;
---
<section id={id} class={className}>
    <div>
        <slot slot="column-one" />
    </div>
</section>